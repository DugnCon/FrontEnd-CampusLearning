name: Deploy User App

on:
  push:
    branches: ["main"]
    paths:
      - '!frontend/user-app/**'    # ko chạy khi user-app thay đổi
      - 'frontend/admin-app/**'  # chạy khi admin-app thay đổi

jobs:
  deploy:
    name: Build & Deploy User FE
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup NodeJS
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: ./frontend/admin-app
        run: npm ci --legacy-peer-deps

      - name: Build FE (Vite)
        working-directory: ./frontend/admin-app
        run: npm run build

      - name: Prepare server (backup + clean old FE)
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            FE_DIR="/var/www/campuslearning-admin/html"
            BACKUP_BASE="/var/www/campuslearning-admin"

            # Xóa toàn bộ backup cũ
            sudo rm -rf $BACKUP_BASE/html_backup_*

            # Tạo backup mới
            BACKUP_DIR="$BACKUP_BASE/html_backup_$(date +%Y%m%d_%H%M%S)"
            sudo cp -r $FE_DIR $BACKUP_DIR || true

            # Clean FE folder
            sudo rm -rf $FE_DIR/*
      
      - name: Upload new build (build)
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "./frontend/admin-app/build/*"
          target: "/var/www/campuslearning-admin/html"
          strip_components: 1

      - name: Restart Nginx
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            sudo systemctl restart nginx